# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store
.vercel
tsconfig.tsbuildinfo

# Package manager consistency — bun é o padrão do projeto
pnpm-lock.yaml
package-lock.json
yarn.lock

# Bibliotecas grandes de assets ficam fora do git — servidas via CDN futuramente
public/obras/
public/loja-discos/
public/radio-222/
*.mp3
*.wav
*.flac
*.ogg
# Exceção: assets já curados de salas que devem ser commitados
!public/galeria/**

# Documentos e briefings efêmeros na raiz do repo
VISION.md
CLAUDE.md
STATE_AUDIT_REPORT.md
